/*
* Generated by @Thrasher66099
*/

// SPDX-License-Identifier: MIT

pragma solidity ^0.8.0;

/*
 * @dev Provides information about the current execution context, including the
 * sender of the transaction and its data. While these are generally available
 * via msg.sender and msg.data, they should not be accessed in such a direct
 * manner, since when dealing with meta-transactions the account sending and
 * paying for execution may not be the actual sender (as far as an application
 * is concerned).
 *
 * This contract is only required for intermediate, library-like contracts.
 */
abstract contract Context {
    function _msgSender() internal view virtual returns (address) {
        return msg.sender;
    }

    function _msgData() internal view virtual returns (bytes calldata) {
        return msg.data;
    }
}

/**
 * @dev Contract module which provides a basic access control mechanism, where
 * there is an account (an owner) that can be granted exclusive access to
 * specific functions.
 *
 * By default, the owner account will be the one that deploys the contract. This
 * can later be changed with {transferOwnership}.
 *
 * This module is used through inheritance. It will make available the modifier
 * `onlyOwner`, which can be applied to your functions to restrict their use to
 * the owner.
 */
abstract contract Ownable is Context {
    address private _owner;

    event OwnershipTransferred(address indexed previousOwner, address indexed newOwner);

    /**
     * @dev Initializes the contract setting the deployer as the initial owner.
     */
    constructor() {
        _setOwner(_msgSender());
    }

    /**
     * @dev Returns the address of the current owner.
     */
    function owner() public view virtual returns (address) {
        return _owner;
    }

    /**
     * @dev Throws if called by any account other than the owner.
     */
    modifier onlyOwner() {
        require(owner() == _msgSender(), "Ownable: caller is not the owner");
        _;
    }

    /**
     * @dev Leaves the contract without owner. It will not be possible to call
     * `onlyOwner` functions anymore. Can only be called by the current owner.
     *
     * NOTE: Renouncing ownership will leave the contract without an owner,
     * thereby removing any functionality that is only available to the owner.
     */
    function renounceOwnership() public virtual onlyOwner {
        _setOwner(address(0));
    }

    /**
     * @dev Transfers ownership of the contract to a new account (`newOwner`).
     * Can only be called by the current owner.
     */
    function transferOwnership(address newOwner) public virtual onlyOwner {
        require(newOwner != address(0), "Ownable: new owner is the zero address");
        _setOwner(newOwner);
    }

    function _setOwner(address newOwner) private {
        address oldOwner = _owner;
        _owner = newOwner;
        emit OwnershipTransferred(oldOwner, newOwner);
    }
}

contract NTHandIdentityMint is Ownable {

    string[] private class = [
        "Hand of Citadel",
        "Hand of Citadel",
        "Hand of Citadel"
    ];
    
    string[] private gender = [
        "Male",
        "Male",
        "Female"
    ];
    
    string[] private race = [
        "Devil",
        "Saint",
        "Survivor"
    ];

    string[] private ability = [
        "Damn",
        "Bless",
        "Invest"
    ];

    string[] private eyes = [
        "Demon",
        "Angelic",
        "Focused"
    ];

    string[] private creditYield = [
        "High",
        "High",
        "High"
    ];

    function setClassArray(uint256 index, string memory value) external onlyOwner
    {
        require(index < 7, "Index out of range");
        class[index] = value;
    }

    function setGenderArray(uint256 index, string memory value) external onlyOwner
    {
        require(index < 7, "Index out of range");
        gender[index] = value;
    }

    function setRaceArray(uint256 index, string memory value) external onlyOwner
    {
        require(index < 7, "Index out of range");
        race[index] = value;
    }

    function setAbilityArray(uint256 index, string memory value) external onlyOwner
    {
        require(index < 7, "Index out of range");
        ability[index] = value;
    }

    function setEyesArray(uint256 index, string memory value) external onlyOwner
    {
        require(index < 7, "Index out of range");
        eyes[index] = value;
    }

    function setCreditYieldArray(uint256 index, string memory value) external onlyOwner
    {
        require(index < 7, "Index out of range");
        creditYield[index] = value;
    }
    
    function getCredits(uint256 tokenId) public view returns (string memory)
    {
        return generateCredits();
    }
    
    function random(string memory input) internal pure returns (uint256) {
        return uint256(keccak256(abi.encodePacked(input)));
    }
    
    function getClass(uint256 tokenId) public view returns (string memory) {
        return generateClass(tokenId);
    }
    
    function getRace(uint256 tokenId) public view returns (string memory) {
        return generateRace(tokenId);
    }
    
    function getStrength(uint256 tokenId) public view returns (string memory) {
        return generateStat();
    }

    function getIntelligence(uint256 tokenId) public view returns (string memory) {
        return generateStat();
    }
    
    function getAttractiveness(uint256 tokenId) public view returns (string memory) {
        return generateStat();
    }
    
    function getTechSkill(uint256 tokenId) public view returns (string memory) {
        return generateStat();
    }
    
    function getCool(uint256 tokenId) public view returns (string memory) {
        return generateStat();
    }

    function getEyes(uint256 tokenId) public view returns (string memory) {
        return generateEyes(tokenId);
    }

    function getGender(uint256 tokenId) public view returns (string memory) {
        return generateGender(tokenId);
    }

    function getAbility(uint256 tokenId) public view returns (string memory) {
        return generateAbility(tokenId);
    }

    function getTextFill(string memory classFill) internal view returns (string memory) {
        return "#FFF569";
    }

    function getBorderFill(string memory raceFill) internal view returns (string memory) {
        return "#FFF569";
    }

    function generateAbility(uint256 tokenId) internal view returns (string memory) {
        return ability[tokenId - 2281];
    }
    
    function generateClass(uint256 tokenId) internal view returns (string memory) {
        return class[tokenId - 2281];
    }

    function generateGender(uint256 tokenId) internal view returns (string memory) {
        return gender[tokenId - 2281];
    }

    function generateEyes(uint256 tokenId) internal view returns (string memory) {
        return eyes[tokenId - 2281];
    }

    function generateRace(uint256 tokenId) internal view returns (string memory) {
        return race[tokenId - 2281];
    }

    function specialRaceCheck(string memory _race) internal view returns (bool) {
        if(strArrayCmp(race, _race, 6, 3))
        {
            return true;
        }

        return false;
    }

    function generateCredits() internal pure returns (string memory) {
        return "0";
    }

    function generateCreditYield() internal pure returns (string memory) {
        return "High";
    }

    function generateStat() internal pure returns (string memory) {
        return "100";
    }

    function packStats(uint256 tokenId) internal view returns (string memory)
    {
        string[16] memory parts;

        parts[0] = '";</text><text x="50" y="180" class="keyword">private int</text><text x="130" y="180" class="base">strength = ';

        parts[1] = getStrength(tokenId);

        parts[2] = ';</text><text x="230" y="180" class="keyword">private int</text><text x="310" y="180" class="base">intelligence = ';

        parts[3] = getIntelligence(tokenId);

        parts[4] = ';</text><text x="50" y="200" class="keyword">private int</text><text x="130" y="200" class="base">attractiveness = ';

        parts[5] = getAttractiveness(tokenId);

        parts[6] = ';</text><text x="320" y="140" class="keyword">private int</text><text x="400" y="140" class="base">techSkill = ';

        parts[7] = getTechSkill(tokenId);

        parts[8] = ';</text><text x="440" y="180" class="keyword">private int</text><text x="520" y="180" class="base">cool = ';       

        parts[9] = getCool(tokenId);

        parts[10] = ';</text><text x="345" y="220" class="keyword">private int</text><text x="425" y="220" class="base">credits = ';

        parts[11] = generateCredits();

        parts[12] = ';</text><text x="50" y="220" class="keyword">private string</text><text x="150" y="220" class="base">characterEyes = "';

        parts[13] = packEndStats(tokenId);

        string memory output = string(abi.encodePacked(parts[0], parts[1], parts[2], parts[3], parts[4], parts[5], parts[6], parts[7], parts[8]));
        output = string(abi.encodePacked(output, parts[9], parts[10], parts[11], parts[12], parts[13]));

        return output;
    }

    function packEndStats(uint256 tokenId) internal view returns (string memory)
    {
        string[5] memory parts;
        parts[0] = generateEyes(tokenId);
        
        parts[1] = '";</text><text x="50" y="140" class="keyword">private string</text><text x="150" y="140" class="base">creditYield = "';

        parts[2] = generateCreditYield();

        parts[3] = '";</text><text x="50" y="240" class="keyword">private string</text><text x="150" y="240" class="base">ability = "';

        parts[4] = generateAbility(tokenId);

        string memory output = string(abi.encodePacked(parts[0], parts[1], parts[2], parts[3], parts[4]));

        return output;
    }

    function packMetaAttributes(uint256 tokenId) internal view returns (string memory)
    {
        string memory attClass = generateClass(tokenId);
        string memory attRace = getRace(tokenId);
        string memory output = string(abi.encodePacked('{"name": "Character #', toString(tokenId), '","attributes": [{"trait_type": "Status", "value": "First Citizen"},{"trait_type": "Class", "value": "', attClass,'"},{"trait_type": "Gender", "value": "', generateGender(tokenId),'"},{"trait_type": "Race", "value": "', attRace, '"},{"trait_type": "Ability", "value": "', getAbility(tokenId),'"},{"trait_type": "Eyes", "value": "'));
        output = string(abi.encodePacked(output, packMetaStats(tokenId)));
        return output;
    }

    function packMetaStats(uint256 tokenId) internal view returns (string memory)
    {
        string memory output = string(abi.encodePacked( generateEyes(tokenId), '"},{"trait_type": "Strength", "max_value": 100, "value": ', getStrength(tokenId), '},{"trait_type": "Intelligence", "max_value": 100, "value": ', getIntelligence(tokenId), '},{"trait_type": "Attractiveness", "max_value": 100, "value": '));
        output = string(abi.encodePacked(output, getAttractiveness(tokenId), '},{"trait_type": "Tech Skill", "max_value": 100, "value": ', getTechSkill(tokenId), '},{"trait_type": "Cool", "max_value": 100, "value": ', getCool(tokenId), '},{"trait_type": "Credits", "value": ', getCredits(tokenId), '},{"trait_type": "Credit Yield", "value": "', generateCreditYield(), '"}],'));
        return output;
    }

    function tokenURI(uint256 tokenId) public view returns (string memory) {
        require(tokenId > 2280 && tokenId < 2288, "Not a hand");
        string[12] memory parts;

        parts[0] = '<svg xmlns="http://www.w3.org/2000/svg" preserveAspectRatio="xMinYMin meet" viewBox="0 0 680 660"><style>.lineCount { fill: #BABABA; font-family: consolas; font-size: 12px; }</style><style>.global { fill: #8B9189; font-family: consolas; font-size: 12px; }</style><style>.keyword { fill: #e0932d; font-family: consolas; font-size: 12px; }</style><style>.base { fill: ';
        
        parts[2] = '; font-family: consolas; font-size: 12px; }</style><rect width="100%" height="100%" fill="';
        
        parts[4] = '" /><rect x="8" y="8" width="97.7%" height="98%" fill="#242925" /><rect x="8" y="8" width="3%" height="98%" fill="#2c302d"/><text x="15" y="20" class="lineCount">1</text><text x="15" y="40" class="lineCount">2</text><text x="15" y="60" class="lineCount">3</text><text x="15" y="80" class="lineCount">4</text><text x="15" y="100" class="lineCount">5</text><text x="15" y="120" class="lineCount">6</text><text x="15" y="140" class="lineCount">7</text><text x="15" y="160" class="lineCount">8</text><text x="15" y="180" class="lineCount">9</text><text x="12" y="200" class="lineCount">10</text><text x="12" y="220" class="lineCount">11</text><text x="12" y="240" class="lineCount">12</text><text x="12" y="260" class="lineCount">13</text><text x="12" y="280" class="lineCount">14</text><text x="12" y="300" class="lineCount">15</text><text x="12" y="320" class="lineCount">16</text><text x="12" y="340" class="lineCount">17</text><text x="12" y="360" class="lineCount">18</text><text x="12" y="380" class="lineCount">19</text><text x="12" y="400" class="lineCount">20</text><text x="12" y="420" class="lineCount">21</text><text x="12" y="440" class="lineCount">22</text><text x="12" y="460" class="lineCount">23</text><text x="12" y="480" class="lineCount">24</text><text x="12" y="500" class="lineCount">25</text><text x="12" y="520" class="lineCount">26</text><text x="12" y="540" class="lineCount">27</text><text x="12" y="560" class="lineCount">28</text><text x="12" y="580" class="lineCount">29</text><text x="12" y="600" class="lineCount">30</text><text x="12" y="620" class="lineCount">31</text><text x="12" y="640" class="lineCount">32</text><text x="30" y="20" class="keyword">using</text><text x="70" y="20" class="base">System;</text><text x="30" y="40" class="keyword">using</text><text x="70" y="40" class="base">System.IO;</text><text x="30" y="60" class="keyword">using</text><text x="70" y="60" class="base">System.Text.Json;</text><text x="30" y="80" class="keyword">using</text><text x="70" y="80" class="base">GameWorkspace.AuthUtils;</text><text x="30" y="120" class="keyword">class</text><text x="68" y="120" class="base">player {</text><text x="270" y="200" class="keyword">private string</text><text x="370" y="200" class="base">characterClass = "';

        parts[5] = generateClass(tokenId);

        parts[6] = '";</text><text x="50" y="160" class="keyword">private string</text><text x="148" y="160" class="base">characterGender = "';

        parts[7] = generateGender(tokenId);

        parts[8] = '";</text><text x="325" y="160" class="keyword">private string</text><text x="425" y="160" class="base">characterRace = "';

        parts[9] = getRace(tokenId);

        //Get both fill colors after deciding race and class
        parts[1] = getTextFill(parts[5]);
        parts[3] = getBorderFill(parts[9]);

        parts[10] = packStats(tokenId);

        parts[11] = '";</text><text x="330" y="240" class="base">};</text><text x="350" y="240" class="lineCount">//DAY ONE UPLOAD IDENTITY</text><text x="30" y="260" class="keyword">int</text><text x="60" y="260" class="base">main(</text><text x="95" y="260" class="keyword">string</text><text x="135" y="260" class="base">[] args) {</text><text x="50" y="280" class="keyword">player</text><text x="98" y="280" class="base">uniqueCharacter = GenerateCharacter();</text><text x="50" y="300" class="base">ApplyCredits(uniqueCharacter);</text><text x="50" y="320" class="keyword">try</text><text x="70" y="320" class="base">{</text><text x="90" y="320" class="keyword">string</text><text x="138" y="320" class="base">json = JsonSerializer.Serialize(uniqueCharacter);</text><text x="70" y="340" class="keyword">if</text><text x="90" y="340" class="base">(json !=</text><text x="150" y="340" class="keyword">null</text><text x="185" y="340" class="base">&amp;&amp; json.Length &gt; 0)</text><text x="315" y="340" class="base">{</text><text x="328" y="340" class="global">File</text><text x="355" y="340" class="base">.WriteAllText(</text><text x="448" y="340" class="global">Environment</text><text x="520" y="340" class="base">.CurrentDirectory</text><text x="90" y="360" class="base">+ "/CharacterStorage/Character-" + tokenId.toString() + ".json", json; } }</text><text x="50" y="380" class="base">catch (SerializationException e) {}</text><text x="50" y="400" class="base">metaverseObjects.Add(uniqueCharacter);</text><text x="50" y="420" class="keyword">WebSocket</text><text x="115" y="420" class="base">=</text><text x="125" y="420" class="keyword">new</text><text x="150" y="420" class="base">WebSocket(wsLocation);</text><text x="50" y="440" class="keyword">Task</text><text x="80" y="440" class="base">_t =</text><text x="110" y="440" class="keyword">await</text><text x="150" y="440" class="base">connection.send("Uploading character data: " + json);</text><text x="50" y="460" class="global">Console</text><text x="98" y="460" class="base">.Writeline("Upload task status :" + _t.Status);</text><text x="50" y="480" class="keyword">while</text><text x="85" y="480" class="base">(!_t.IsCompleted) { Thread.Sleep(500); }</text><text x="50" y="500" class="global">Console</text><text x="98" y="500" class="base">.Writeline("Upload task completed");</text><text x="50" y="520" class="base">CreateCharacterInterfaceHeaders(uniqueCharacter, 0, 0, housingParams[], *networkArray);</text><text x="50" y="540" class="base">ValidateAuthToken(uniqueCharacter, housingParams[], *networkArray);</text><text x="50" y="560" class="base">VerifyCharacterLinkedList(**database.Storage.CLL.(uniqueCharacter), *networkArray);</text><text x="50" y="580" class="global">Console</text><text x="98" y="580" class="base">.Writeline(PlayerStateCheck(uniqueCharacter));</text><text x="50" y="600" class="global">Console</text><text x="98" y="600" class="base">.Writeline("Welcome Citizen!");</text><text x="50" y="620" class="keyword">return</text><text x="98" y="620" class="base">0;</text><text x="30" y="640" class="base">}</text></svg>';

        string memory output = string(abi.encodePacked(parts[0], parts[1], parts[2], parts[3], parts[4], parts[5], parts[6], parts[7], parts[8]));
        output = string(abi.encodePacked(output, parts[9], parts[10], parts[11]));

        string memory strAttributes = packMetaAttributes(tokenId);
        
        string memory json = Base64.encode(bytes(string(abi.encodePacked(strAttributes, '"description": "Welcome to Neo Tokyo, citizen. Are you prepared to fulfill your destiny?", "image": "data:image/svg+xml;base64,', Base64.encode(bytes(output)), '"}'))));
        output = string(abi.encodePacked('data:application/json;base64,', json));

        return output;
    }
    
    function toString(uint256 value) internal pure returns (string memory) {
    // Inspired by OraclizeAPI's implementation - MIT license
    // https://github.com/oraclize/ethereum-api/blob/b42146b063c7d6ee1358846c198246239e9360e8/oraclizeAPI_0.4.25.sol

        if (value == 0) {
            return "0";
        }
        uint256 temp = value;
        uint256 digits;
        while (temp != 0) {
            digits++;
            temp /= 10;
        }
        bytes memory buffer = new bytes(digits);
        while (value != 0) {
            digits -= 1;
            buffer[digits] = bytes1(uint8(48 + uint256(value % 10)));
            value /= 10;
        }
        return string(buffer);
    }

    function memcmp(bytes memory a, bytes memory b) internal pure returns(bool){
        return (a.length == b.length) && (keccak256(a) == keccak256(b));
    }

    function strcmp(string memory a, string memory b) internal pure returns(bool){
        return memcmp(bytes(a), bytes(b));
    }

    /*
    * Created by @Thrasher66099 to check if a string exists in an array of strings.
    */
    function strArrayCmp(string[] memory a, string memory b, uint startIndex, uint count) internal pure returns(bool){

        for(uint i = startIndex; i < (startIndex + count); i++)
        {
            if(strcmp(a[i], b))
            {
                return true;
            }
        }

        return false;
    }
    
    constructor() {}
}


/// [MIT License]
/// @title Base64
/// @notice Provides a function for encoding some bytes in base64
/// @author Brecht Devos <brecht@loopring.org>
library Base64 {
    bytes internal constant TABLE = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/";

    /// @notice Encodes some bytes to the base64 representation
    function encode(bytes memory data) internal pure returns (string memory) {
        uint256 len = data.length;
        if (len == 0) return "";

        // multiply by 4/3 rounded up
        uint256 encodedLen = 4 * ((len + 2) / 3);

        // Add some extra buffer at the end
        bytes memory result = new bytes(encodedLen + 32);

        bytes memory table = TABLE;

        assembly {
            let tablePtr := add(table, 1)
            let resultPtr := add(result, 32)

            for {
                let i := 0
            } lt(i, len) {

            } {
                i := add(i, 3)
                let input := and(mload(add(data, i)), 0xffffff)

                let out := mload(add(tablePtr, and(shr(18, input), 0x3F)))
                out := shl(8, out)
                out := add(out, and(mload(add(tablePtr, and(shr(12, input), 0x3F))), 0xFF))
                out := shl(8, out)
                out := add(out, and(mload(add(tablePtr, and(shr(6, input), 0x3F))), 0xFF))
                out := shl(8, out)
                out := add(out, and(mload(add(tablePtr, and(input, 0x3F))), 0xFF))
                out := shl(224, out)

                mstore(resultPtr, out)

                resultPtr := add(resultPtr, 4)
            }

            switch mod(len, 3)
            case 1 {
                mstore(sub(resultPtr, 2), shl(240, 0x3d3d))
            }
            case 2 {
                mstore(sub(resultPtr, 1), shl(248, 0x3d))
            }

            mstore(result, encodedLen)
        }

        return string(result);
    }
}